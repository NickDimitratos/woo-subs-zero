# Repository Instructions

## Documentation Lookup

Use Context7 MCP to fetch current documentation whenever a task needs library, framework, SDK, API, CLI, or cloud-service details. Start with `resolve-library-id`, then query the selected library docs.

## Pull Requests

- Do not prefix PR titles with `[codex]`.
- Every PR must include a plugin version bump and changelog entry.
